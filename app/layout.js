export const metadata = {
  title: "IRIS — The Future, Through IRIS",
  description: "Powerful. IRIS is your gateway to next‑gen technology."
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
